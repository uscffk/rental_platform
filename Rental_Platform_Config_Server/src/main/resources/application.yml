server:
  port: 3344
spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/ffk123456/rental_-platform-config-center.git
          search-paths: Rental_Platform-ConfigCenter
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/